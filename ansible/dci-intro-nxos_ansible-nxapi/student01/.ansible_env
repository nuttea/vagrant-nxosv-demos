# Setup environment for ansible playbooks
# Credentials for DCI NXOSv Pod

# usage: source .ansible_env

# Authentication for ansible NXOS network modules
export ANSIBLE_NET_USERNAME=admin
export ANSIBLE_NET_PASSWORD=admin
